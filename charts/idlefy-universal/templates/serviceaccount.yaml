{{- range $deploymentName, $deploymentConfig := .Values.deployments }}
{{- $renderSA := or $deploymentConfig.autoCreateServiceAccount (and $deploymentConfig.serviceAccount (not $deploymentConfig.autoCreateServiceAccount)) }}
{{- if $renderSA }}
{{- $saCfg := default dict $deploymentConfig.serviceAccount }}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- include "idlefy-universal.resourceMetadata" (dict "name" ($saCfg.name | default $deploymentName) "labelName" $deploymentName "root" $ "labels" $saCfg.labels "annotations" $saCfg.annotations) | nindent 2 }}
{{- if hasKey $saCfg "automountServiceAccountToken" }}
automountServiceAccountToken: {{ $saCfg.automountServiceAccountToken }}
{{- end }}
{{- $genericPull := list }}
{{- if and $.Values.generic $.Values.generic.extraImagePullSecrets }}
{{- $genericPull = $.Values.generic.extraImagePullSecrets }}
{{- end }}
{{- $pullSecrets := concat (default list $saCfg.imagePullSecrets) $genericPull }}
{{- if $pullSecrets }}
imagePullSecrets:
  {{- toYaml $pullSecrets | nindent 2 }}
{{- end }}
---
{{- end }}
{{- end }}
{{- range $stsName, $stsConfig := .Values.statefulSets }}
{{- $renderSA := or $stsConfig.autoCreateServiceAccount (and $stsConfig.serviceAccount (not $stsConfig.autoCreateServiceAccount)) }}
{{- if $renderSA }}
{{- $saCfg := default dict $stsConfig.serviceAccount }}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- include "idlefy-universal.resourceMetadata" (dict "name" ($saCfg.name | default $stsName) "labelName" $stsName "root" $ "labels" $saCfg.labels "annotations" $saCfg.annotations) | nindent 2 }}
{{- if hasKey $saCfg "automountServiceAccountToken" }}
automountServiceAccountToken: {{ $saCfg.automountServiceAccountToken }}
{{- end }}
{{- $genericPull := list }}
{{- if and $.Values.generic $.Values.generic.extraImagePullSecrets }}
{{- $genericPull = $.Values.generic.extraImagePullSecrets }}
{{- end }}
{{- $pullSecrets := concat (default list $saCfg.imagePullSecrets) $genericPull }}
{{- if $pullSecrets }}
imagePullSecrets:
  {{- toYaml $pullSecrets | nindent 2 }}
{{- end }}
---
{{- end }}
{{- end }}
{{- range $dsName, $dsConfig := .Values.daemonSets }}
{{- $renderSA := or $dsConfig.autoCreateServiceAccount (and $dsConfig.serviceAccount (not $dsConfig.autoCreateServiceAccount)) }}
{{- if $renderSA }}
{{- $saCfg := default dict $dsConfig.serviceAccount }}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- include "idlefy-universal.resourceMetadata" (dict "name" ($saCfg.name | default $dsName) "labelName" $dsName "root" $ "labels" $saCfg.labels "annotations" $saCfg.annotations) | nindent 2 }}
{{- if hasKey $saCfg "automountServiceAccountToken" }}
automountServiceAccountToken: {{ $saCfg.automountServiceAccountToken }}
{{- end }}
{{- $genericPull := list }}
{{- if and $.Values.generic $.Values.generic.extraImagePullSecrets }}
{{- $genericPull = $.Values.generic.extraImagePullSecrets }}
{{- end }}
{{- $pullSecrets := concat (default list $saCfg.imagePullSecrets) $genericPull }}
{{- if $pullSecrets }}
imagePullSecrets:
  {{- toYaml $pullSecrets | nindent 2 }}
{{- end }}
---
{{- end }}
{{- end }}
