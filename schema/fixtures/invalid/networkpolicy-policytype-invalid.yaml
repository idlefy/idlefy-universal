# expected_error: is not one of ['Ingress', 'Egress']
deployments:
  api:
    containers:
      main: {image: api, imageTag: "1.0", ports: {http: {containerPort: 8080}}}
    autoCreateNetworkPolicy: true
    networkPolicy:
      policyTypes: [Foo]
      ingress: []
