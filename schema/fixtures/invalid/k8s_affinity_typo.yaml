# expected_error: Additional properties are not allowed
deployments:
  app:
    containers:
      web:
        image: nginx
        imageTag: "1.27"
        ports:
          http: {containerPort: 80}
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - {key: node-type, operator: In, values: [memory-optimized]}
      nodeSelectorr: {}  # typo — must fail validation now that affinity is typed
