# expected_error: Additional properties are not allowed
deployments:
  app:
    containers:
      web:
        image: nginx
        imageTag: "1.27"
        ports:
          http: {containerPort: 80}
        lifecycle:
          preStop:
            exec:
              command: [/bin/sh, -c, sleep 10]
            httpGetWrongName:    # typo — must fail
              path: /shutdown
              port: 80
